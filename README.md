# URL 跳转工具

基于 Vue 3 + Vite + TypeScript + Element Plus + Tailwind CSS 的轻量级 URL 跳转配置工具。

## 开发

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```

样例配置：`public/jump-config.json`（可在应用内「导入配置」）。

需求说明见 `prompt.md`。
